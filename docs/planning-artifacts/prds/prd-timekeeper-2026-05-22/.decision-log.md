# Decision Log — TimeKeeper PRD

Canonical memory and audit trail for the `bmad-prd` run. Records decisions, overrides, and rationale as the artifact was produced.

| # | Date | Decision | Rationale | Captured in |
|---|---|---|---|---|
| 1 | 2026-05-22 | **Run mode: headless Fast-path.** No interactive Discovery; produced the full PRD with `[ASSUMPTION]` tags and `open_questions`. | User instruction: "this is all the input you're getting from me … do bmad and get a PRD output." Per `references/headless.md`: do not ask; infer and record. | run mode |
| 2 | 2026-05-22 | **Intent: Create** (no prior PRD); workspace bound to `docs/planning-artifacts/prds/prd-timekeeper-2026-05-22/`. | Matches `prd_output_path`/`run_folder_pattern` from `customize.toml`; `planning_artifacts` set to `docs/planning-artifacts` at install. | workspace |
| 3 | 2026-05-22 | **Brief = the `research/` folder** (findings, product-scope, data-model, mcp-server) — extracted directly into the PRD. | Research is complete (per project memory); it is the supplied product spec. `project_knowledge` was set to `research` at install. | source_inputs frontmatter |
| 4 | 2026-05-22 | **Stakes: internal tool**, hackathon outcome. Length above the 5–8pp internal default because the MCP/agent surface is a genuine API concern. | Rubric dimension 7 (shape fit) + PRD discipline (length scales with concerns). | overall depth |
| 5 | 2026-05-22 | **Working product name: "TimeKeeper."** Flagged as working title. | User said "Harvest time keeper"; needs confirmation to avoid trademark collision. | §title, OQ-1 |
| 6 | 2026-05-22 | **Four personas, incl. AI Agent as a first-class run-time persona.** | The hackathon's hard requirement (agents operate via MCP) makes the Agent a peer interface, not an integration. Kept to ≤4 to avoid persona theater (rubric dim. 2). | §2 |
| 7 | 2026-05-22 | **Scoped to the product + run-time agent surface only.** Build-time agentic SDLC framework declared a Non-Goal. | Avoids conflating the two agent layers (findings §1); the framework is a separate initiative (project memory: agentic-sdlc-asis-subsystem). | §0, §8 |
| 8 | 2026-05-22 | **Event-native + MCP-native framed as the thesis/differentiator**, not an add-on. | Findings §3: real Harvest has no event/agent layer; this is the strategic bet (rubric dim. 3). | §1, §4.6, §4.7 |
| 9 | 2026-05-22 | **MVP = MoSCoW "Must" set + the MCP loop.** Approval (FR-15/16), reminders (FR-22), MCP notifications (FR-26), timestamp mode, rate engine, web-UI breadth → deferred. | product-scope §4 backlog + findings §5; keep the signature demo (UJ-5) central. | §9 |
| 10 | 2026-05-22 | **Budgets are hours-only in MVP; full rate/billing engine deferred** with the entire invoicing/expenses cluster. | data-model §4 (trim) + findings §5; billing is the clearest scope line. | §4.2, §7, §8 |
| 11 | 2026-05-22 | **Recommendations recorded for the 5 research open questions** (API shape, tracking mode, multi-tenancy, agent auth, deployment) rather than decided unilaterally. | findings §7 already carries recommendations; these are team decisions, kept genuinely open (rubric dim. 1/5). | §11 OQ-2,3,4,7,8 |
| 12 | 2026-05-22 | **Technical-how moved to `addendum.md`** (stack, transport, full 25-tool/9-resource catalogue, schema, rejected alternatives, build sequence). | PRD discipline: capabilities in PRD, implementation in addendum. | addendum.md |
| 13 | 2026-05-22 | **Counter-metrics added** (tool count, feature breadth, event volume not to be optimized). | Rubric dim. 3 requires counter-metrics when SMs exist; guards against gaming the agent surface. | §10 SM-C1..C3 |
| 14 | 2026-05-22 | **Install note:** BMAD bmm installed non-interactively; installer threw a benign Windows `EBUSY` on temp cleanup after content + 44 Claude Code skills + configs landed. | Recorded so a future `bmad status`/update isn't surprised by the partial-cleanup. | n/a (ops) |
| 15 | 2026-05-22 | **Finalize:** ran the Reviewer Gate (rubric walker) as a subagent; addressed findings; set `status: final`. | BMAD Finalize steps 3 & 7. | see review-rubric.md |

## Design step (2026-05-22) — `bmad-create-ux-design` + frontend-design

| # | Decision | Rationale | Captured in |
|---|---|---|---|
| D1 | Ran the BMAD UX-design workflow (all 14 steps) headless as Sally, layered with the `frontend-design` skill's aesthetic bar. | User: "invoke the impeccable design skill and run the bmad design step." Autonomous mode continues. | ux-design-specification.md |
| D2 | **Hard mandate: token system only, ZERO inline CSS.** Two-tier pipeline: `tokens.json` (W3C DTCG source) → `tokens.css` (CSS custom properties). Components consume semantic tokens via `var()`; lint-enforced. | Direct user instruction. Verified the artifacts contain no `style=` attributes. | tokens.json, tokens.css, UX spec §13 |
| D3 | Aesthetic direction **"Ledger & Dial"** — precision instrument + editorial ledger. Teal brand accent; **red reserved for danger/over-budget only** (so "running" ≠ "over budget"); Fraunces + IBM Plex Sans + IBM Plex Mono (tabular numerals for all time/money). Light default + dark theme. | frontend-design: pick a bold, intentional direction; avoid AI-slop; fit a daily data tool. Hue-separation is an a11y win. | UX spec §4, §7 |
| D4 | **Resolved PRD OQ-5:** human web UI IS in scope (UJ-1–UJ-4 get screens); UJ-5 agent loop stays headline, made visible via provenance chip. | The design ask presupposes a UI; the agent surface stays primary. | prd.md §11 OQ-5, §13 |
| D5 | **Added §13 Design & UX to the PRD** (Aesthetic & Tone, IA, platform, mandate) + design-mandate constraint in §6; folded in via BMAD Adapt-In clusters rather than renumbering FRs. | User: "add design to PRD." Avoids churn to FR/SM/OQ IDs. | prd.md §6, §13 |
| D6 | Design open items recorded, not invented: default-theme behavior (DQ-1), token build tool (DQ-2), component layer tech (DQ-3). | Genuinely team/architecture decisions. | UX spec §14 |

## Architecture step (2026-05-22) — `bmad-create-architecture` → `docs/planning-artifacts/architecture.md`

| # | Decision | Rationale | Captured in |
|---|---|---|---|
| A1 | Ran the BMAD architecture workflow (all 8 steps) headless as the architect. | User: "continue" → next step in the chain. | architecture.md |
| A2 | **Verified current tech versions via web (mandated by the step)** — updates the dated addendum: MCP spec 2025-06-18→**2025-11-25**; SDK = **`@modelcontextprotocol/sdk` 1.29.x only** (Streamable HTTP built in; `@modelcontextprotocol/node` obsolete); **Node 24 LTS**, **Prisma 7**, **PostgreSQL 18**; web **Vite 8 + React 19**. | Step rule "ALWAYS search the web to verify versions." | architecture.md §0.1; addendum §A note |
| A3 | **Modular monolith, one deployable process**: MCP `/mcp` + REST `/api` + static SPA, all over a shared **service layer** that owns behavior + permissions. | Structurally guarantees PRD NFR-4 (MCP/REST parity); hackathon-simple (addendum co-location). | architecture.md §3.1 |
| A4 | **No meta-framework starter** — thin pnpm monorepo (`apps/server`, `apps/web`, `packages/{design-tokens,shared}`). | Keeps the MCP server independently runnable/demoable; small surface. | architecture.md §2, §5 |
| A5 | **Frontend = Vite 8 + React 19 + CSS Modules consuming design tokens; zero inline CSS, lint-gated in CI.** | Resolves the addendum's deferred UI tech under the [[design-tokens-only-no-inline-css]] mandate. | architecture.md §3.5, §4.6 |
| A6 | Recorded consistency patterns for AI-agent implementers (DB snake_case / API camelCase / MCP snake_case tool names / event dot.snake) + uniform error envelope + Zod-at-boundary. | Step 5 goal: prevent multi-agent divergence. | architecture.md §4 |
| A7 | Deferred (post-hackathon), not dropped: OAuth 2.1, MCP notifications (FR-26), rate snapshots, multi-tenant, stateless horizontal scaling. Tech open Qs AQ-1..3 left open. | Honest scope; matches PRD deferrals. | architecture.md §3, §6 |

## Epics & Stories step (2026-05-22) — `bmad-create-epics-and-stories` → `docs/planning-artifacts/epics.md`

| # | Decision | Rationale | Captured in |
|---|---|---|---|
| E1 | Ran all 4 steps headless as product strategist. **4 epics, 21 stories**, organized by user value. Final validation passed: all FR-1..26 covered, no forward deps, foundation folded into Epic 1.1 (no pure-technical epic). | User: "do the next bmad." BMAD epic rules (value not layers; standalone epics; tables-when-needed). | epics.md |
| E2 | **Epics 1+2 = the signature demo (UJ-5):** E1 Time Capture & Agent Loop (FR-1..6), E2 Reporting & Budgets (FR-17..19). E3 Workspace Management & Access (FR-7..14). E4 Governance & Event-Native (FR-15,16,20,21,22,26) marked Should/Stretch. | Front-loads the demo per architecture build sequence + PRD §9. | epics.md Epic List |
| E3 | FR-23/24/25 (MCP) treated as **cross-cutting, realized progressively** (each capability adds its tools); transport+auth baseline in Epic 1. FR-13 baseline (own-data) in Epic 1, full role enforcement + REST/MCP parity test in Story 3.6 (validates SM-4). | Architecture's shared service layer makes MCP a per-capability concern, not a final epic. | epics.md coverage map |
| E4 | Story 1.1 = scaffold + MCP `ping` (architecture's mandated first story) framed as agent-connectivity value + themed shell. Each UI story carries token-only/no-inline-CSS + AA as testable ACs. | Reconciles BMAD "no technical-only stories" with the walking-skeleton requirement; enforces [[design-tokens-only-no-inline-css]]. | epics.md Story 1.1 + UX ACs |
